let init,runScript;_a06‍.w("./lib/scraper",[["init",["init"],function(v){init=v}],["runScript",["runScript"],function(v){runScript=v}]]);

// credentials
const credentials = {
    username: 'sums.co',
    password: 'niubige101'
}

// search options
const searchOptions = {
    name: "wang",
    numOfCharacters: 2,
    suffix: false
}

// time between each page load, default 10 seconds.
const timeInterval = 10000

init(searchOptions, function() {
    runScript(credentials, timeInterval)
});

